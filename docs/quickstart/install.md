---
id: install
title: Install
---

Prerequisites:

- .NET SDK (for C#) or JDK (for Java)
- NVIDIA CUDA toolkit (for CUDA backend) and compatible GPU drivers, if targeting GPU
- C++ toolchain (clang/LLVM or MSVC depending on OS)

Install steps:

- Obtain Hybridizer toolchain license and installer.
- Add Hybridizer CLI to PATH.
- Verify installation:

```bash
hybridizer --version
```

Next: Hello GPU.
